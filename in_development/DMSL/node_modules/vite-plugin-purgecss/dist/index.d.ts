import { UserDefinedOptions } from 'purgecss';
import { OutputOptions, EmittedAsset, OutputChunk } from 'rollup';

declare const _default: (options: Partial<UserDefinedOptions>) => {
    name: string;
    enforce: string;
    transformIndexHtml(html: string): void;
    generateBundle(_options: OutputOptions, bundle: {
        [fileName: string]: EmittedAsset | OutputChunk;
    }): Promise<void>;
};

export { _default as default };
